#include "deck.h"
#include <stdio.h>

/**
 * compare - compare two cards
 * @card1: a card
 * @card2: a card
 *
 * Return:
 * -1 if card1 is less than card 2
 *  1 if card1 is greater than card2
 *  0 otherwise
 */
int compare(const deck_node_t *card1, const deck_node_t *card2)
{
}

/**
 * sort_deck - sort a deck of cards
 * @deck: a double pointer to the head of a doubly-linked list of cards
 */
void sort_deck(deck_node_t **deck)
{
}
